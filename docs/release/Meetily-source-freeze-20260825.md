# Meetily 源码冻结与本地验收记录（2026-08-25）

## 1. 冻结结论

- 冻结分支：`codex/meetily-i18n-release-source`
- 源码冻结提交：`78d07f57970ecde6003ba586d8cb33dc929356a1`
- 提交主题：`feat(release): freeze accepted templates transcription and summary source`
- 提交时间：`2026-08-25T20:23:09+08:00`
- 冻结范围：多语言、中文转录归一化、录音启动与恢复、重新转录、摘要生成与编辑历史、自定义会议模板、Word/JSON 导入、模板包导入导出，以及对应数据库迁移、自动化测试、QA 工具和 Windows 发布运行库。
- 源码结论：冻结前源码门禁通过，可以进入“从干净提交重新构建本地验收发布件”。
- 发布边界：本记录只放行本地验收构建；面向外部分发仍须补齐 Windows Authenticode 签名和 Tauri 更新包签名。

## 2. 冻结边界审计

### 2.1 纳入提交

- 共 `249` 个文件：`91` 个新增、`158` 个修改。
- 差异规模：`27,571` 行新增、`3,016` 行删除。
- 纳入了产品源码、数据库迁移、测试、构建配置、审计脚本、QA 脚本及干净构建所需的 Windows 应用本地运行库。

### 2.2 明确排除

- `docs/i18n/audit/**` 下 `660` 个未跟踪文件：历史运行证据，只保留在本机，不进入源码提交。
- 仓库根目录 `target-*`、`target-*.zip`、`.tools`、`downloads`：本机构建、探针、恢复和下载输出。
- Python `__pycache__` 与 `*.pyc`：本地解释器缓存。

冻结时未发现上述历史证据之外的遗漏源码文件；未执行 `git clean`、`git reset` 或批量删除，原有证据与构建产物均保留。

## 3. 测试结果

### 3.1 前端逻辑与回归测试

- Node/TSX 兼容测试：`21` 个测试文件，`129` 项通过，`0` 项失败。
- Bun 专用测试：`2` 个测试文件，`8` 项通过，`0` 项失败；运行时为 Bun `1.4.0`。
- 前端合计：`23` 个测试文件，`137` 项通过，`0` 项失败。
- 覆盖重点包括：多语言键值、摘要语言偏好、自定义模板导入与绑定、模板包、录音启动门禁、应用重启后的自动录音防冒泡/防重复启动、摘要编辑和相关纯逻辑回归。

### 3.2 TypeScript 与生产前端构建

- `pnpm exec tsc --noEmit`：通过，退出码 `0`。
- `pnpm build`：通过，Next.js 编译、类型与 lint 门禁通过，静态页面 `13/13` 生成成功。

### 3.3 Rust/Tauri/Vulkan 核心测试

- 命令：`cargo test -p meetily --test app_lib_tests --features vulkan`
- 结果：`366` 项通过，`0` 项失败，`2` 项按测试定义忽略。
- 首次运行的唯一失败原因为测试进程未在 `PATH` 中发现已下载并校验的 FFmpeg；将构建产出的 FFmpeg 目录加入测试进程 `PATH` 后，先单项复验通过，再重跑全部 `368` 项并得到上述最终结果。
- 该环境修正没有修改产品逻辑，也没有把本机路径写入源码。
- 重点覆盖：录音检查点与恢复、音频导入、重新转录、中文简繁归一化、VAD、Whisper/Vulkan 加速、摘要生成生命周期、手工编辑历史、模板 CRUD、Word 导入、模板包事务/冲突/取消/恢复/安全边界和数据库一致性。

### 3.4 格式与差异门禁

- `git diff --cached --check`：通过。
- `cargo fmt --all -- --check`：通过。
- 冻结提交前未暂存差异：`0`；历史审计证据之外的未跟踪文件：`0`。

## 4. 安全与发布输入审计

- 对冻结候选文本执行强特征凭据和本机绝对路径扫描。
- 命中项经人工分类：一个原仓库已存在且内容未变化的分析服务客户端标识；两个用于验证模板包隐私扫描会拒绝敏感文本的测试夹具。
- 未发现新增真实私钥、访问令牌、API 密钥、密码、Bearer 凭据或 `C:\Users\liuxin` / `D:\桌面\meetlily` 本机路径泄漏。
- 大于 `10 MiB` 的冻结候选只有 `DirectML.dll`（约 `17.67 MiB`），属于 Windows Vulkan/DirectML 发布运行库。
- Windows 运行库清单共校验 `11` 个 DLL：文件字节数与清单一致，SHA-256 全部一致；`DirectML.dll` Authenticode 状态为 `Valid`，签发者为 Microsoft Windows Publisher。

## 5. 下一门禁

必须从上述冻结提交创建干净的 Git 工作树，在该工作树中重新安装锁定依赖、构建真实 `llama-helper`、执行 Vulkan Tauri/NSIS 构建，并对最终复制到 `target/release` 的 EXE 和安装程序重新计算 SHA-256、检查签名状态、使用真实数据库启动并核对迁移、语言、模型预热、Vulkan 后端及致命日志。

只有干净工作树重建与真实启动冒烟通过后，本地验收发布件才可替换此前由脏工作区生成的候选件。
